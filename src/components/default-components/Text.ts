import { colors, fontSize } from '@/styles/themes'
import styled from 'styled-components/native'

export const Text = styled.Text`
  color: ${colors.white};
  font-size: ${fontSize.sm};
  font-family: 'Questrial_400Regular';
  opacity: 0.3;
`
